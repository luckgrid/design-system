# Packages

- [`styles`](styles/README.md) contains the authored CSS source and the one
  public-preview entrypoint.

The repository does not currently publish a package. Compatibility is defined
by `exports.tsv` and the inventories at the repository root, not by directory
visibility.
